Create new mashine Centos 7

Create new user

adduser vitalii
passwd vitalii

usermod -aG wheel vitalii

Install epel

sudo yum install epel-release

sudo yum install ansible

sudo yum upgrade

ssh-keygen -t rsa -b 4096

cd .ssh

ssh-copy-id -i id_rsa.pub user@host



ansible_ssh_private_key_file: /home/vitalii/.ssh/id_rsa


sudo yum remove jenkins
